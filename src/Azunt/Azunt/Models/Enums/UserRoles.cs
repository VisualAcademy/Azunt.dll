namespace Azunt.Models.Enums
{
    /// <summary>
    /// 기본 제공되는 사용자 역할 (ASP.NET Core Identity 기반)
    /// </summary>
    public enum Roles
    {
        Administrators,
        Everyone,
        Users,
        Guests
    }
}
